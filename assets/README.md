# assets

<p align="center">  <samp>
    <strong>中文</strong> ·
    <a href="./README.en.md">English</a>
  </samp>
</p>

存放制作或使用 Skill 时需要复用的静态资源和模板。

当前内容：

- `evals-template.json`：新 Skill 的评测文件模板。

放入这里的文件应当能被多个流程复用，或能稳定约束某种输出格式。操作步骤写在 `SKILL.md` 或 `references/`，不要堆进 `assets/`。
